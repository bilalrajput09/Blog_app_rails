<header>
    <nav class="navbar">
        <ul class="nav_list">
            <% if user_signed_in? %>
                <li class="nav_items">
                    <%= link_to "Users", users_path%>
                </li>
                <li class="nav_items">
                    <%= link_to "Create post", new_user_post_path(user_id: current_user.id)%>
                </li>
                <li class="nav_items">
                    <%= link_to "My profile", user_path(id: current_user.id)%>
                </li>
            <% end %>
        </ul>
    </nav>
</header>
