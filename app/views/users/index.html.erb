<% if user_signed_in? %>
    <div>Welcome <%= current_user.email %></div>
    <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn-signout" %>

<% else %>
    <%= button_to "Sign in", new_user_session_path, class: "btn-signout" %>
<% end %>

<%= render @users %>